import React, { FC } from 'react';
import {
  ResumeStyled
} from './Resume.styled';

interface Resume_type {

};

const Resume: FC<any> = (props) => {

  return (
    <ResumeStyled>

    </ResumeStyled>
  );
};

export default Resume;